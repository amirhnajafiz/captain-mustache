    redis:
        image: 'bitnami/redis:latest'
        environment:
          - ALLOW_EMPTY_PASSWORD=yes
        networks:
          - mustache
        volumes:
          - 'mustache-volume:/bitnami/redis'
